<!-- managed by Doctorine — edits will be overwritten -->
# Versioning

Doctorine authors Conventional Commits for `github.com/sdk-fixture-lab-260904/parcel-events-nightly-go`: `feat!` for breaking changes, `feat` for additive features, `fix` for compatible fixes, and `chore` for version-only synchronization.

Release Please owns version and changelog changes on `main`. An exact Release-As override is accepted only after Doctorine verifies its bound footer on the base branch.

Auto-merge seed: `non-breaking`. Managed furniture is overwritten on regeneration; customer code outside managed paths remains customer-owned.
